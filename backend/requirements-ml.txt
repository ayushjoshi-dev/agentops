# ============================================================
# AgentOps — Machine Learning Dependencies
# 
# These dependencies are separated from requirements.txt because
# they are large and sometimes require special installation
# flags (like PyTorch CPU-only).
# 
# Install with:
#   pip install -r requirements-ml.txt
# ============================================================

# Local embedding model (free, no API key needed)
# Uses ONNX runtime, very low RAM footprint compared to PyTorch
fastembed
