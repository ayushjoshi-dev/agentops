[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    live: marks tests as requiring real LLM/DB (skip with -m 'not live')
    integration: marks integration tests requiring full app startup
addopts = -v --tb=short
