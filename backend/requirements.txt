# ============================================================
# AgentOps — Core Backend Dependencies
# ============================================================
# 
# Install with:  pip install -r requirements.txt
# Then for ML:   pip install -r requirements-ml.txt
#
# Split into two files because sentence-transformers/torch is ~1GB.
# Install core first to get the server running fast.
# ============================================================

# ── Web Framework ─────────────────────────────────────────
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.32.0,<0.34.0
python-multipart>=0.0.12

# ── Pydantic ──────────────────────────────────────────────
pydantic[email]>=2.9.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0

# ── Database ──────────────────────────────────────────────
sqlalchemy>=2.0.30,<2.1.0
# psycopg v3 — pre-compiled Windows wheels, no pg_config needed
psycopg[binary]>=3.2.0,<4.0.0
alembic>=1.13.0,<2.0.0

# ── Authentication ────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── AI / LLM ──────────────────────────────────────────────
langchain>=0.3.10
langchain-community>=0.3.10
langgraph>=0.2.50
langchain-groq>=0.2.1
langchain-openai>=0.2.10
langchain-text-splitters>=0.3.3

# ── pgvector ──────────────────────────────────────────────
pgvector>=0.3.0

# ── Document Processing ───────────────────────────────────
pypdf>=5.0.0

# ── HTTP / Async ──────────────────────────────────────────
httpx>=0.27.0
aiofiles>=24.0.0

# ── Utilities ─────────────────────────────────────────────
python-dotenv>=1.0.0
structlog>=24.4.0
tenacity>=8.5.0

# NOTE: sentence-transformers (embeddings) is in requirements-ml.txt
# It pulls PyTorch (~1GB). Install separately after core is working.
